---
trigger: always_on
---

if the MCP server code has changed, the MCP server in AntiGravity will require a restart, before trying to test the new code through the MCP protocol, stop and notify that the MCP server needs restarting, and then resume.
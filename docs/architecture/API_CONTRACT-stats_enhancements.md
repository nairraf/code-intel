# Archived Plan: get_stats Enhancements

## Status

This document is retained as historical context from the pre-reboot roadmap.

It is not an authoritative implementation plan for the reboot branch.

## What Still Carries Forward

The reboot still values the core ideas captured here:

- index freshness visibility
- git-aware trust signals
- architectural rule validation
- project health summaries for agents

## Where The Reboot Rehomes Those Ideas

Under the reboot direction, these ideas are now split across:

- `get_index_status` in `docs/architecture/API_CONTRACT-core.md`
- `get_stats` in `docs/architecture/API_CONTRACT-core.md`
- the preferred storage and trust model in `docs/architecture/REBOOT_ARCHITECTURE.md`
- the active roadmap in `docs/PROJECT_PLAN.md`
- the active execution plan in `docs/architecture/IMPLEMENTATION_PLAN-structural-context-pivot.md`

## Guidance

Do not implement against this document directly on the reboot branch.

Use it only as reference material when preserving or adapting prior work.
